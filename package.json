{
  "name": "portfolio-generator",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "description": "Monorepo for a customizable portfolio generator with Next.js full‑stack, Vite playground and shared packages.",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "prisma": "^6.12.0",
    "ts-node": "^10.9.2",
    "turbo": "^2.5.5",
    "typescript": "^5.0.4"
  }
}