{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && prisma generate --schema=./prisma/schema.prisma && cd apps/web && pnpm run build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".next"
}
