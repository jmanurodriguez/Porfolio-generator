# Variables de entorno para Vercel
# Configura estas en tu Vercel Dashboard → Settings → Environment Variables

# Base de datos (usar servicio externo como Railway, Neon, Supabase)
DATABASE_URL=postgresql://user:password@host:port/database

# NextAuth
NEXTAUTH_SECRET=tu_secret_de_32_caracteres_aleatorios
NEXTAUTH_URL=https://tu-app.vercel.app

# S3 Storage (usar Cloudflare R2, AWS S3, etc.)
S3_ENDPOINT=https://tu-bucket.r2.cloudflarestorage.com
S3_ACCESS_KEY=tu_access_key
S3_SECRET_KEY=tu_secret_key
S3_BUCKET_NAME=tu-bucket-name

# SMTP (usar SendGrid, Resend, etc.)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=tu_sendgrid_api_key

# Opcional: Para debugging
NODE_ENV=production
